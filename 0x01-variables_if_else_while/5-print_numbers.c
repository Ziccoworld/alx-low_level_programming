#include <stdio.h>

/**
 * main - prints number from 0 to 9.
 *
 * Return: Alwalys 0.
 */
int main(void)
{
        int i;

	for (i = 0; < 10; i++)
	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}
